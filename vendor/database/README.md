Vendored copy of https://github.com/moneymanagerex/database (`tables.sql`).

Refresh:

```bash
curl -fsSL -o vendor/database/tables.sql \
  https://raw.githubusercontent.com/moneymanagerex/database/master/tables.sql
```

Used to create empty `.mmb` files and to document the `_V1` schema. Do not edit by hand.
